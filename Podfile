# Uncomment the next line to define a global platform for your project
# platform :ios, '8.0'

target 'InstanstMessageDemo' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  # use_frameworks!

  # Pods for InstanstMessageDemo
    pod 'NIMSDK',    '~> 3.2.0'
    pod 'SDWebImage', '~> 4.0.0-beta2'

  target 'InstanstMessageDemoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'InstanstMessageDemoUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
